<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="ValidationCode">
        <attr name="CodeCount" format="integer" />
        <attr name="PointNumber" format="integer" />
        <attr name="LineNumber" format="integer" />
        <attr name="CodeTextSize" format="dimension" />
        <attr name="BackGround" format="color" />
    </declare-styleable>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>

    <declare-styleable name="CircleImageView">
        <attr name="border_width" format="dimension" />
        <attr name="border_color" format="color" />
    </declare-styleable>

    <declare-styleable name="searchedit">
        <attr name="imagewidth" format="dimension" />
        <attr name="textColorEdit" format="color" />
        <attr name="textSizeEdit" format="dimension" />
        <attr name="etText" format="string"></attr>
    </declare-styleable>


    <declare-styleable name="SlideDetailsLayout">
        <!-- float value for indicate the moment of switch panel-->
        <attr name="percent" format="float" />
        <!-- how long the animation keep-->
        <attr name="duration" format="integer" />
        <!-- default panel to show after init-->
        <attr name="default_panel" format="enum">
            <enum name="front" value="0" />
            <enum name="behind" value="1" />
        </attr>
    </declare-styleable>

    <declare-styleable name="AccountView">
        <attr name="av_width" format="dimension"></attr>
        <attr name="av_height" format="dimension"></attr>
        <attr name="av_text" format="string"></attr>
    </declare-styleable>

    <declare-styleable name="SegmentControl">

        <attr name="cornerRadius" format="dimension|reference" />

        <attr name="normalColor" format="color|reference" />

        <attr name="selectedColor" format="color|reference" />

        <attr name="backgroundColors" format="color|reference" />

        <attr name="textColors" format="color|reference" />

        <attr name="texts" format="string|reference" />

        <attr name="android:textSize" />

        <attr name="android:orientation" />

        <!-- 外部边框的width -->


        <attr name="boundWidth" format="dimension|reference" />

        <!-- 内部边框的width -->


        <attr name="separatorWidth" format="dimension|reference" />

        <attr name="gaps" format="dimension|reference" />

        <attr name="horizonGap" format="dimension|reference" />

        <attr name="verticalGap" format="dimension|reference" />

    </declare-styleable>
    <declare-styleable name="PickerView">

        <attr name="isLoop" format="boolean" />

    </declare-styleable>

    <declare-styleable name="DatePicker">
        <attr name="picker_text_color" format="color" />
        <attr name="picker_select_textColor" format="color" />
        <attr name="picker_split" format="color" />
        <attr name="picker_split_height" format="dimension" />
    </declare-styleable>

</resources>